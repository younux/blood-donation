import { MyTypeaheadDirective } from './my-typeahead.directive';

describe('MyTypeaheadDirective', () => {
  it('should create an instance', () => {
    const directive = new MyTypeaheadDirective();
    expect(directive).toBeTruthy();
  });
});
